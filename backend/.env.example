# Backend — never commit actual secrets
HF_TOKEN=your_huggingface_token_here

# Token Rotation Pool (comma-separated, optional)
# Add multiple tokens to scale quota limits.
# HF_TOKENS=token1,token2,token3
